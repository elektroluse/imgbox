services:
  db:
    image: postgres:16
    ports:
          - "5432:5432"
    restart: always

    environment:
      POSTGRES_PASSWORD: notagoodpassword!

  adminer:
    image: adminer:latest
    restart: always
    ports:
      - "8888:8080"